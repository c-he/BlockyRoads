


// Instantiate the module
Random instance_name (
    .clk(clk), 
    .direction(direction), 
    .car_on(car_on), 
    .path(path), 
    .speed_data(speed_data), 
    .obstacle_num(obstacle_num)
    );


